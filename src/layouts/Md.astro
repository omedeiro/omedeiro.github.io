---
import Base from "./Base.astro";
const { frontmatter } = Astro.props;
---
<Base title={frontmatter.title + " — Owen Medeiros"} description={frontmatter.description} noindex={frontmatter.noindex}>
  <h1>{frontmatter.title}</h1>
  <slot />
</Base>
